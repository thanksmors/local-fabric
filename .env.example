# Copy this file to .env for local-only secrets.
# Never commit .env or paste secret values into agent output.

# Preferred provider for external research when local tooling supports it.
PERPLEXITY_API_KEY=
